# Agent guide

Preserve Studio → Runtime → Mint boundaries. Never invent asset provenance. Never represent registry manifests as character cards. Prefer deterministic validators over prose review. Run `pnpm check` before claiming success.
