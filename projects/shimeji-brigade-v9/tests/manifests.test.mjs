import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';for(const id of ['mizu','kurogami'])test(`${id} manifest has seven states`,()=>{const m=JSON.parse(fs.readFileSync(`assets/sprites/${id}/manifest.json`));assert.deepEqual(Object.keys(m.states),['idle','walk','climb','fall','drag','interact','perch']);assert.equal(m.frameSize.width,128);assert.equal(m.frameSize.height,128)});
