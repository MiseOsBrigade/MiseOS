{
  "name": "@miseos/proof-registry-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@miseos/chaingate-core": "workspace:*",
    "@miseos/agent-tools": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8",
    "@types/node": "^22.10.5"
  },
  "main": "dist/workflow.js",
  "types": "src/workflow.ts",
  "exports": { ".": { "types": "./src/workflow.ts", "import": "./dist/workflow.js" } }
}
